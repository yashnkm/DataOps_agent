# Gradio Framework — pinned to 5.x (v4 conflicts with modern huggingface_hub,
# v6 has breaking Svelte rendering changes)
gradio>=5,<6

# Google Gemini SDK (still used by rag_processor and database layer)
google-generativeai>=0.8.0

# LangChain 1.x ecosystem (modern create_agent() API)
langchain>=1.2,<2
langchain-core>=1.2,<2
langchain-community>=0.4,<1
langchain-google-genai>=4.2,<5

# LangGraph + MCP (langgraph version is governed by langchain 1.x — leave loose)
langgraph>=1.0,<2
langchain-mcp-adapters>=0.2,<1

# Embeddings & Reranking (sentence-transformers pulls torch + transformers)
sentence-transformers

# Vector Store & Search
faiss-cpu
rank-bm25
scikit-learn

# Document Processing
pypdf
pdfplumber
python-docx
openpyxl
pandas
numpy

# Database
psycopg2-binary
sqlalchemy

# Utilities
python-dotenv
